{
  "name": "vanilla-agentic-framework",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "dev": "npm run build -- -w",
    "example": "npm run build && node dist/examples/mathAgent.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.5.0"
  }
}
